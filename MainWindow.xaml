<Window x:Class="sample_wpf.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="OpenAI Chat Console"
        Height="600"
        Width="900"
        MinWidth="700"
        MinHeight="500">
    <TabControl Margin="10">
        <TabItem Header="Chat">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Type your prompt below and press Send to call the OpenAI API."
                           FontSize="18"
                           Margin="0,0,0,10"
                           TextWrapping="Wrap"/>

                <TextBox x:Name="PromptTextBox"
                         Grid.Row="1"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         FontSize="16"
                         Padding="10"
                         ToolTip="Enter the prompt to send to OpenAI"/>

                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,10">
                    <TextBlock x:Name="StatusTextBlock"
                               VerticalAlignment="Center"
                               FontStyle="Italic"
                               Foreground="Gray"
                               Margin="0,0,10,0"
                               Text="Ready."/>
                    <Button x:Name="SendButton"
                            Width="120"
                            Content="Send"
                            Click="SendButton_Click"
                            Padding="10,5"/>
                </StackPanel>

                <TextBlock Grid.Row="3"
                           Text="Response:"
                           FontWeight="Bold"
                           Margin="0,10,0,5"/>

                <TextBox x:Name="ResponseTextBox"
                         Grid.Row="4"
                         AcceptsReturn="True"
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         FontSize="16"
                         Padding="10"
                         Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"/>
            </Grid>
        </TabItem>

        <TabItem Header="Todo List">
            <Grid Margin="20" DataContext="{Binding ElementName=RootWindow}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Track quick todos right inside the app."
                           FontWeight="Bold"
                           FontSize="18"
                           Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            Margin="0,0,0,10">
                    <TextBox x:Name="TodoInputTextBox"
                             Width="400"
                             FontSize="16"
                             Padding="8"
                             ToolTip="Enter a todo item"/>
                    <Button Content="Add"
                            Margin="10,0,0,0"
                            Padding="10,5"
                            Click="AddTodo_Click"/>
                </StackPanel>

                <ListBox x:Name="TodoListBox"
                         Grid.Row="2"
                         ItemsSource="{Binding Todos}"
                         FontSize="16">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1"
                                    BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"
                                    Padding="5">
                                <DockPanel LastChildFill="True">
                                    <Button Content="Remove"
                                            DockPanel.Dock="Right"
                                            Margin="10,0,0,0"
                                            Padding="6,3"
                                            Click="RemoveTodo_Click"/>
                                    <TextBlock Text="{Binding}"
                                               VerticalAlignment="Center"
                                               TextWrapping="Wrap"/>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
